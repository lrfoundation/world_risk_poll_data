# Python requirements for the report reproduction scripts.
#   pip install -r reports/requirements.txt
pandas>=2.0
pyarrow>=14
statsmodels>=0.14   # regression models in a few reports
openpyxl>=3.1       # only for reports/external/fetch_external.py (Excel downloads)
